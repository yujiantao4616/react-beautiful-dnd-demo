import ReactDOM from 'react-dom'
import React from 'react'
import Instruction from './instruction'
import './style.css'

const rootElement = document.getElementById("root");

ReactDOM.render(<Instruction />, rootElement);